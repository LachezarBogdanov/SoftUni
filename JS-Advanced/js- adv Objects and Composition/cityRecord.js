function cityRecord (name, population, treasury){

    let cityInfo = {
        name,
        population,
        treasury
    }

    return cityInfo
}
console.log(cityRecord('Tortuga',
7000,
15000));